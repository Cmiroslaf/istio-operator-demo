apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: todo
spec:
  hosts:
  - 'localhost'
  gateways:
  - todo-gateway
  http:
  - name: "todo-routes"
    match:
    - uri:
        prefix: /todo
    route:
    - destination:
        port:
          number: 8080
        host: todo
#      corsPolicy:
#        allowOrigin:
#        - "*"
#        allowMethods:
#        - POST
#        - GET
#        - OPTIONS
#        - PUT
#        - DELETE
#        - PATCH
#        allowHeaders:
#        - "authorization"
#        - "dnt"
#        - "user-agent"
#        - "x-requested-with"
#        - "if-modified-since"
#        - "cache-control"
#        - "content-type"
#        - "range"
#        - "x-xsrf-token"
#        exposeHeaders:
#        - "content-length"
#        - "content-range"
#        maxAge: "86400s"
