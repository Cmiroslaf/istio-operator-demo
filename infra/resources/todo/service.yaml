apiVersion: v1
kind: Service
metadata:
  name: todo-svc
  labels:
    app: todo
spec:
  type: NodePort
  ports:
  - port: 8080
    protocol: TCP
    name: http
    targetPort: 8080
  selector:
    app: todo
