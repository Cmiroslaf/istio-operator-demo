apiVersion: v1
kind: Service
metadata:
  name: database
  labels:
    app: database
spec:
  type: ClusterIP
  ports:
  - port: 5432
    protocol: TCP
    name: cloudsql
    targetPort: 5432
  selector:
    app: database
