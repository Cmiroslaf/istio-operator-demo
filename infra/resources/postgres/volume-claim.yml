apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-persistent-claim
  labels:
    app: database
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
